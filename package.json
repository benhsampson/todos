{
  "name": "@todos/root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "lint": "turbo run lint",
    "build": "turbo run build",
    "deploy": "turbo run deploy",
    "format": "prettier -w .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "turbo": "^1.2.5",
    "husky": "^7.0.0"
  }
}
