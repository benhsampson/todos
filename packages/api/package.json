{
  "name": "@todos/api",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "lint": "eslint src",
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "build:watch": "yarn build --watch",
    "start": "node dist",
    "start:watch": "nodemon dist -q --on-change-only",
    "dev": "run-p build:watch start:watch",
    "deploy": "echo \"DEPLOYING!\""
  },
  "devDependencies": {
    "@types/node": "^17.0.27",
    "esbuild": "^0.14.38",
    "nodemon": "^2.0.15",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@todos/shared": "0.0.0"
  }
}
